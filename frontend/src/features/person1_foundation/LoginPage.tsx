// LoginPage.tsx — P1 stub (implemented in TASK LOGIN-1)
import React from 'react'

export default function LoginPage() {
  // TODO (P1 Sprint 1): POST /login form → store JWT → redirect to /dashboard
  return (
    <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', height: '100vh', background: '#f1f5f9' }}>
      <div style={{ padding: 40, background: '#fff', borderRadius: 12, boxShadow: '0 4px 24px rgba(0,0,0,0.08)' }}>
        <h1 style={{ fontSize: 22, fontWeight: 700, marginBottom: 24 }}>QEMS — Login</h1>
        <p style={{ color: '#94a3b8' }}>Login page — to be implemented in TASK LOGIN-1 (Sprint 1).</p>
      </div>
    </div>
  )
}
